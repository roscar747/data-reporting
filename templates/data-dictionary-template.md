<!--
  DATA DICTIONARY TEMPLATE
  ------------------------
  One section per table consumed by the report. Copy a table block and fill it in.
  Goal: a reader who has never seen the data should be able to read this file and
  build a sensible model from scratch.
-->

# Data dictionary — {{Project Title}}

## Overview

| Item | Value |
|------|-------|
| Project | {{Project Title}} |
| Source files | [`../data/raw/`](../data/raw/) |
| Generated by | {{e.g. synthetic data generator script — name and date}} |
| Time range | {{2023-01-01 … 2026-03-31}} |
| Total tables | {{N}} |
| Synthetic? | Yes |

## Entity relationship overview

```text
{{ASCII or Mermaid sketch of the model. Example below.}}

dim_customer ───< fact_transaction >─── dim_merchant
                       │
                       └── dim_date
```

The relationships above are enforced inside the Power BI semantic model. See `BJF_Group_Revenue_Operation_Report.SemanticModel/definition/relationships.tmdl` for the canonical definition (when a `.pbip` is published alongside).

---

## Table: `{{table_name}}`

**Description.** {{One sentence: what real-world entity does each row represent?}}

**Grain.** One row per {{e.g. transaction, patient-visit, settlement-instruction, day-and-feeder}}.

**Row count.** ~{{N}} rows.

**Source file.** [`../data/raw/{{filename}}.csv`](../data/raw/{{filename}}.csv)

**Primary key.** `{{column_name}}` — {{description of uniqueness guarantee}}.

| Column | Type | Required | Description | Example | Notes / business rule |
|--------|------|----------|-------------|---------|------------------------|
| `{{column_a}}` | string | yes | {{What it is in plain English}} | `TX-2024-000001` | {{Format / regex / domain}} |
| `{{column_b}}` | int | yes | {{…}} | `42` | |
| `{{column_c}}` | decimal(18,2) | yes | {{…}} | `1234.56` | Currency: {{GBP / USD / EUR}} |
| `{{column_d}}` | datetime | yes | {{…}} | `2025-03-14T09:42:11Z` | UTC; convert in Power Query if needed |
| `{{column_e}}` | bool | no | {{…}} | `true` | Default `false` when missing |
| `{{column_f}}` | enum | yes | {{…}} | `APPROVED` | One of: `PENDING`, `APPROVED`, `DECLINED`, `REVIEW` |

**Known data-quality quirks.**

- {{e.g. ~0.5% of rows have a null `customer_id` to simulate ingestion gaps. Filtered out at the Power Query stage.}}
- {{e.g. `amount` is sign-agnostic — credits are positive and debits are negative, both stored in the same column.}}

**How it's used in the report.**

- {{Page / visual}} — {{role of this table, e.g. fact for revenue calculations}}
- {{Page / visual}} — {{role}}

---

## Table: `{{another_table_name}}`

(Repeat the section above for every table.)

---

## Reference: column type conventions

| Convention | What it means |
|------------|----------------|
| `string` | Variable-length text, treat as categorical unless the description says otherwise. |
| `int` | 32-bit integer. Values larger than 2 billion will need `int64`. |
| `decimal(p,s)` | Fixed-precision decimal — `p` total digits, `s` after the decimal point. Use for money. |
| `datetime` | ISO 8601, UTC. Power Query converts to local time only where business rules require it. |
| `enum` | A closed list of allowed values. The list is exhaustive — anything else is a data-quality issue. |
| `bool` | `true` / `false` (lowercase in CSV). Treat null as the default value documented per column. |

## Change log

| Date | Change | Author |
|------|--------|--------|
| {{YYYY-MM-DD}} | Initial version. | {{Name}} |
